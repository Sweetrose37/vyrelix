/**
 * Compatibility facade for shared utilities used by future feature modules.
 */
export { createId, debounce, escapeHTML, focusableWithin, trapFocusWithin } from "../utilities/helpers.js";
export { validateRequired, validateLength } from "../utilities/validators.js";
